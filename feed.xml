<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>FW Blog</title>
  <subtitle>FW 的繁體中文技術部落格，分享 Web 開發、API 整合、自動化、Docker 與靜態網站的實作經驗、踩坑紀錄和可重現的技術研究。</subtitle>
  <link href="https://imfw.io/feed.xml" rel="self"/>
  <link href="https://imfw.io/"/>
  <updated>2026-07-21T00:00:00Z</updated>
  <id>https://imfw.io/</id>
  <author>
    <name>FW</name>
  </author>
  
  
  <entry>
    <title>正式環境為什麼需要 Rootless Docker？從 root 權限風險到可驗證部署</title>
    <link href="https://imfw.io/posts/2026/2026-07-21-production-rootless-docker/"/>
    <updated>2026-07-21T00:00:00Z</updated>
    <id>https://imfw.io/posts/2026/2026-07-21-production-rootless-docker/</id>
    <content type="html">&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;查核資訊：&lt;/strong&gt; 本文於 2026-07-21 依 Docker 官方文件與 &lt;code&gt;skill-rootless-docker&lt;/code&gt; 專案內容查核。Docker 支援版本、套件庫指令與各 Linux 發行版的安全限制可能變動，實際修改主機前請再次確認官方文件。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;很多 Docker 教學都會把這行指令列為安裝後的第一件事：&lt;/p&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token function&quot;&gt;sudo&lt;/span&gt; &lt;span class=&quot;token function&quot;&gt;usermod&lt;/span&gt; &lt;span class=&quot;token parameter variable&quot;&gt;-aG&lt;/span&gt; &lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; &lt;span class=&quot;token string&quot;&gt;&quot;&lt;span class=&quot;token environment constant&quot;&gt;$USER&lt;/span&gt;&quot;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;登出再登入，之後執行 &lt;code&gt;docker compose up -d&lt;/code&gt; 就不用一直輸入 &lt;code&gt;sudo&lt;/code&gt;。操作變方便了，看起來也像是「讓一般使用者管理 Docker」。&lt;/p&gt;
&lt;p&gt;問題是，&lt;strong&gt;不用輸入 &lt;code&gt;sudo&lt;/code&gt;，不代表你沒有 root 權限&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://docs.docker.com/engine/install/linux-postinstall/&quot;&gt;Docker 官方的 Linux post-installation 文件&lt;/a&gt;直接警告：&lt;code&gt;docker&lt;/code&gt; group 會授予使用者 root-level privileges。原因不是群組名稱特別危險，而是傳統 Docker Engine 的 daemon 本身以 root 執行；只要能控制它的 socket，就能要求它建立容器、掛載 host 路徑、操作網路與啟動高權限 workload。&lt;/p&gt;
&lt;p&gt;在自己的開發機上，這個取捨有時可以接受。但到了長時間對外服務、保存正式資料，而且可能被 CI/CD 或管理工具操作的 production host，這條權限捷徑值得重新檢查。&lt;/p&gt;
&lt;p&gt;我把這套檢查、安裝、遷移與驗證流程整理成 &lt;a href=&quot;https://github.com/FWcloud916/skill-rootless-docker&quot;&gt;&lt;code&gt;skill-rootless-docker&lt;/code&gt;&lt;/a&gt;。它的目標不只是把 Rootless Docker 裝起來，而是避免 agent 在沒有盤點 workload、沒有說明影響、也沒有 rollback 的情況下，直接改掉一台正式主機。&lt;/p&gt;
&lt;p&gt;這篇先回答最重要的問題：Rootless Docker 究竟降低了什麼風險？哪些 production workload 適合用？又要怎麼確認它真的部署完成，而不只是 installer 顯示 success？&lt;/p&gt;
&lt;h2&gt;問題不在 &lt;code&gt;sudo&lt;/code&gt;，而在 daemon 的權限&lt;/h2&gt;
&lt;p&gt;Docker CLI 並不直接建立 container。當你執行 &lt;code&gt;docker run&lt;/code&gt; 或 &lt;code&gt;docker compose up&lt;/code&gt;，CLI 會透過 Docker API 把要求送給 daemon，再由 daemon 執行真正的工作。&lt;/p&gt;
&lt;p&gt;傳統 rootful Docker 的關係大致是：&lt;/p&gt;
&lt;pre class=&quot;language-text&quot;&gt;&lt;code class=&quot;language-text&quot;&gt;developer / deployer
        │ Docker CLI
        ▼
/var/run/docker.sock
        │
        ▼
dockerd（host root）
        │
        └─ 建立 containers、掛載 host 路徑、設定網路與 cgroup&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Unix socket 可以限制「誰能連線」，卻不會把連線後能呼叫的 Docker API 自動變成低權限版本。&lt;a href=&quot;https://docs.docker.com/engine/security/&quot;&gt;Docker Engine security 文件&lt;/a&gt;也特別舉例：能控制 daemon 的使用者，可以啟動一個把 host &lt;code&gt;/&lt;/code&gt; 掛進 container 的 workload，進而修改 host filesystem。&lt;/p&gt;
&lt;p&gt;因此，把 deployment account 加進 &lt;code&gt;docker&lt;/code&gt; group，本質上不是授予「部署這個 app」的權限，而是授予「控制 rootful daemon」的權限。差距非常大。&lt;/p&gt;
&lt;p&gt;這也解釋了另一個常見誤會：&lt;/p&gt;
&lt;pre class=&quot;language-yaml&quot;&gt;&lt;code class=&quot;language-yaml&quot;&gt;&lt;span class=&quot;token key atrule&quot;&gt;volumes&lt;/span&gt;&lt;span class=&quot;token punctuation&quot;&gt;:&lt;/span&gt;
  &lt;span class=&quot;token punctuation&quot;&gt;-&lt;/span&gt; /var/run/docker.sock&lt;span class=&quot;token punctuation&quot;&gt;:&lt;/span&gt;/var/run/docker.sock&lt;span class=&quot;token punctuation&quot;&gt;:&lt;/span&gt;ro&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;:ro&lt;/code&gt; 只讓 container 不能改寫 socket 這個 filesystem entry，不會把送進 socket 的 &lt;code&gt;POST /containers/create&lt;/code&gt; 等 API request 變成唯讀。只要應用程式能對 socket 發出有效要求，它仍然可以控制 daemon。若某個 dashboard、CI runner 或自動更新工具真的需要 Docker API，必須把它視為 daemon administrator；只需要少量操作時，應考慮有 allowlist 的 socket proxy，而不是因為寫了 &lt;code&gt;:ro&lt;/code&gt; 就當作風險消失。&lt;/p&gt;
&lt;h2&gt;Rootless Docker 改變了哪一層？&lt;/h2&gt;
&lt;p&gt;&lt;a href=&quot;https://docs.docker.com/engine/security/rootless/&quot;&gt;Docker 的 Rootless mode&lt;/a&gt; 會把 Docker daemon 和 containers 一起放進 user namespace，兩者都不以 host root 執行。&lt;/p&gt;
&lt;pre class=&quot;language-text&quot;&gt;&lt;code class=&quot;language-text&quot;&gt;deployer（一般使用者）
        │ Docker CLI / rootless context
        ▼
/run/user/&lt;uid&gt;/docker.sock
        │
        ▼
dockerd（由 deployer 擁有，位於 user namespace）
        │
        └─ containers（仍受同一個非 root 權限邊界限制）&lt;/uid&gt;&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;picture&gt;&lt;source type=&quot;image/webp&quot; srcset=&quot;https://imfw.io/assets/img/bvmubw4_Jg-300.webp 300w, https://imfw.io/assets/img/bvmubw4_Jg-600.webp 600w, https://imfw.io/assets/img/bvmubw4_Jg-1200.webp 1200w&quot; sizes=&quot;(min-width: 30em) 50vw, 100vw&quot; /&gt;&lt;img src=&quot;https://imfw.io/assets/img/bvmubw4_Jg-300.jpeg&quot; alt=&quot;Rootful Docker 與 Rootless Docker 的權限邊界比較，顯示 socket、daemon 與 container 在 host root 或非 root user namespace 下的差異。&quot; loading=&quot;lazy&quot; decoding=&quot;async&quot; width=&quot;1200&quot; height=&quot;800&quot; srcset=&quot;https://imfw.io/assets/img/bvmubw4_Jg-300.jpeg 300w, https://imfw.io/assets/img/bvmubw4_Jg-600.jpeg 600w, https://imfw.io/assets/img/bvmubw4_Jg-1200.jpeg 1200w&quot; sizes=&quot;(min-width: 30em) 50vw, 100vw&quot; /&gt;&lt;/picture&gt;&lt;/p&gt;
&lt;p&gt;user namespace 會把 container 看到的 UID/GID 映射到 host 上的一段 subordinate IDs。Container 裡的 process 可以看起來是 UID 0，但那個 UID 0 不等於 host 的 UID 0。&lt;/p&gt;
&lt;p&gt;Rootless Docker 需要 &lt;code&gt;newuidmap&lt;/code&gt;、&lt;code&gt;newgidmap&lt;/code&gt;，而且目標使用者在 &lt;code&gt;/etc/subuid&lt;/code&gt; 與 &lt;code&gt;/etc/subgid&lt;/code&gt; 都要有至少 65,536 個 subordinate IDs。這些 range 不能靠複製教學裡常見的 &lt;code&gt;100000:65536&lt;/code&gt; 隨便填；正式主機可能已有其他使用者、container runtime 或集中式身分管理配置，重疊的 range 必須先排除。&lt;/p&gt;
&lt;h3&gt;它和「container 內不用 root」不同&lt;/h3&gt;
&lt;p&gt;在 Dockerfile 寫 &lt;code&gt;USER app&lt;/code&gt; 是很好的做法，但它只控制 container 內應用程式用哪個帳號執行。若背後仍是 rootful daemon，daemon 本身依然持有 host root 權限。&lt;/p&gt;
&lt;p&gt;兩者應該疊加，而不是二選一：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Container 內使用 non-root user，降低應用程式被入侵後能做的事。&lt;/li&gt;
&lt;li&gt;Docker daemon 使用 rootless mode，降低 daemon 或 runtime 邊界被突破後對 host 的影響。&lt;/li&gt;
&lt;li&gt;再搭配最小 capabilities、seccomp、唯讀 filesystem、secrets 管理、image 更新與網路限制。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;它和 &lt;code&gt;userns-remap&lt;/code&gt; 也不同&lt;/h3&gt;
&lt;p&gt;&lt;a href=&quot;https://docs.docker.com/engine/security/userns-remap/&quot;&gt;&lt;code&gt;userns-remap&lt;/code&gt;&lt;/a&gt; 同樣會重新映射 container 內的 UID/GID，但 Docker 官方文件指出，這個模式下 daemon 仍以 root 執行。Rootless mode 則連 daemon 本身都不持有 root 權限。&lt;/p&gt;
&lt;p&gt;所以兩者處理的是不同深度的邊界：&lt;code&gt;userns-remap&lt;/code&gt; 主要降低 container process 對 host UID 的權限；rootless 更進一步移除 daemon 的 host root 身分。&lt;/p&gt;
&lt;h2&gt;為什麼 production 特別需要評估 rootless？&lt;/h2&gt;
&lt;p&gt;我不會說每一台 production host 都能無條件改成 rootless。Kubernetes node、需要 host device 的 workload、複雜網路設備或依賴特定 kernel controller 的服務，都需要另外設計。&lt;/p&gt;
&lt;p&gt;但對常見的「一台 Linux VM 加 Docker Compose」部署，Rootless Docker 應該從額外 hardening 選項，提升為預設評估項。&lt;/p&gt;
&lt;h3&gt;1. Production 的暴露時間與資料價值都更高&lt;/h3&gt;
&lt;p&gt;開發機可能只在工作時啟動，資料也能重建；production daemon 則長時間執行、接收外部流量、掛載 persistent volumes，還可能保存 database、上傳檔案與 deployment credentials。&lt;/p&gt;
&lt;p&gt;Rootless 不會修掉應用程式漏洞，但當攻擊鏈跨過 container 或 Docker runtime 邊界時，daemon 沒有 host root 身分，能減少攻擊者直接取得整台主機最高權限的機會。這是 defense in depth：它不假設上一層永遠不會失敗，而是限制失敗之後的 blast radius。&lt;/p&gt;
&lt;h3&gt;2. Deployment account 不必同時成為系統管理員&lt;/h3&gt;
&lt;p&gt;理想的 production 權限分工是：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;administrator 負責套件、kernel、firewall、SSH 與 system service；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;deployer&lt;/code&gt; 負責自己的 rootless daemon 與 applications；&lt;/li&gt;
&lt;li&gt;application account 只取得執行服務需要的檔案與 secret。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;日常部署因此不必使用 &lt;code&gt;sudo&lt;/code&gt;，也不必讓 &lt;code&gt;deployer&lt;/code&gt; 加入 rootful &lt;code&gt;docker&lt;/code&gt; group。真的需要 host-level 變更時，再由獨立 administrator 執行一組已審核、可 rollback 的命令。&lt;/p&gt;
&lt;p&gt;這個差異在自動化環境尤其重要。CI credential 外洩時，如果它只能控制某個 deployment account 的 rootless daemon，和它能控制 host root daemon，是兩種完全不同的事故範圍。&lt;/p&gt;
&lt;h3&gt;3. 權限邊界會變得更容易說明與稽核&lt;/h3&gt;
&lt;p&gt;Rootless daemon 的 socket 通常位於 &lt;code&gt;/run/user/&amp;lt;uid&amp;gt;/docker.sock&lt;/code&gt;，資料預設放在 &lt;code&gt;~/.local/share/docker&lt;/code&gt;，設定則位於 &lt;code&gt;~/.config/docker&lt;/code&gt;。Service 由 &lt;code&gt;systemctl --user&lt;/code&gt; 管理。&lt;/p&gt;
&lt;p&gt;這讓「誰擁有 daemon、誰能連 socket、資料放在哪裡、服務如何啟動」都能對應到一個明確的 Linux account。比起所有 deployment automation 共用 &lt;code&gt;/var/run/docker.sock&lt;/code&gt;，權限模型更容易被檢查。&lt;/p&gt;
&lt;p&gt;不過別把 rootless socket 當成普通 IPC。拿到它的人依然能控制該使用者的所有 rootless containers，也可能讀到 containers 能存取的 secrets 與 bind mounts。Rootless 降低的是 host privilege，不是把 daemon API 變成低風險 API。&lt;/p&gt;
&lt;h2&gt;為什麼我把流程做成一個 skill？&lt;/h2&gt;
&lt;p&gt;Rootless Docker 的安裝命令不長，真正麻煩的是命令前後的決策。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;主機是不是原生 Linux，而且以 systemd 運作？&lt;/li&gt;
&lt;li&gt;要沿用既有 account，還是建立專用 &lt;code&gt;deployer&lt;/code&gt;？&lt;/li&gt;
&lt;li&gt;目前有沒有 rootful containers、volumes 或 systemd services？&lt;/li&gt;
&lt;li&gt;新舊 daemon 要 coexist，還是正式 migration？&lt;/li&gt;
&lt;li&gt;Workload 是否依賴 80/443、Docker socket、bind mounts、devices 或嚴格的 cgroup limits？&lt;/li&gt;
&lt;li&gt;User service 能不能跨過 logout，並在 reboot 後自動啟動？&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如果 agent 一看到「幫我裝 Rootless Docker」就直接跑安裝，很容易把 audit 變成 mutation，甚至停掉還在提供服務的 rootful daemon。&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/FWcloud916/skill-rootless-docker&quot;&gt;&lt;code&gt;rootless-docker-setup&lt;/code&gt;&lt;/a&gt; 把工作拆成固定順序：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Read-only preflight&lt;/strong&gt;：先收集 host 與 target user 狀態，不安裝套件、不用 &lt;code&gt;sudo&lt;/code&gt;、不修改 service。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Change plan&lt;/strong&gt;：把已正確、必要、可選與不相容項目分開，列出命令、影響與 rollback。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Explicit approvals&lt;/strong&gt;：套件、daemon migration、networking、access hardening 分組確認。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Target-user installation&lt;/strong&gt;：setup tool 一律由真正的 non-root login session 執行。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;End-to-end validation&lt;/strong&gt;：檢查 context、socket、process owner、Compose、user service、linger、container 與對外 port。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;picture&gt;&lt;source type=&quot;image/webp&quot; srcset=&quot;https://imfw.io/assets/img/bkTjyuDxlX-300.webp 300w, https://imfw.io/assets/img/bkTjyuDxlX-600.webp 600w, https://imfw.io/assets/img/bkTjyuDxlX-1200.webp 1200w&quot; sizes=&quot;(min-width: 30em) 50vw, 100vw&quot; /&gt;&lt;img src=&quot;https://imfw.io/assets/img/bkTjyuDxlX-300.jpeg&quot; alt=&quot;Rootless Docker 安全部署流程，從只讀 preflight、變更計畫、明確核准、目標使用者安裝到端對端驗證，失敗時停止診斷或回滾。&quot; loading=&quot;lazy&quot; decoding=&quot;async&quot; width=&quot;1200&quot; height=&quot;800&quot; srcset=&quot;https://imfw.io/assets/img/bkTjyuDxlX-300.jpeg 300w, https://imfw.io/assets/img/bkTjyuDxlX-600.jpeg 600w, https://imfw.io/assets/img/bkTjyuDxlX-1200.jpeg 1200w&quot; sizes=&quot;(min-width: 30em) 50vw, 100vw&quot; /&gt;&lt;/picture&gt;&lt;/p&gt;
&lt;p&gt;可以透過 &lt;code&gt;skills&lt;/code&gt; CLI 安裝：&lt;/p&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;npx skills &lt;span class=&quot;token function&quot;&gt;add&lt;/span&gt; FWcloud916/skill-rootless-docker &lt;span class=&quot;token punctuation&quot;&gt;&#92;&lt;/span&gt;
  &lt;span class=&quot;token parameter variable&quot;&gt;--skill&lt;/span&gt; rootless-docker-setup&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;或安裝成 Codex plugin：&lt;/p&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;codex plugin marketplace &lt;span class=&quot;token function&quot;&gt;add&lt;/span&gt; FWcloud916/skill-rootless-docker
codex plugin &lt;span class=&quot;token function&quot;&gt;add&lt;/span&gt; rootless-docker@fwcloud916&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;接著不要一開始就要求它安裝，先要求 audit：&lt;/p&gt;
&lt;pre class=&quot;language-text&quot;&gt;&lt;code class=&quot;language-text&quot;&gt;Use $rootless-docker-setup to audit this Ubuntu host for rootless Docker
readiness for user deployer. Do not change the host.&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;這句 &lt;code&gt;Do not change the host&lt;/code&gt; 並不是因為 skill 本身沒有安全規則，而是讓任務意圖再次變得明確：現在是盤點，不是安裝。&lt;/p&gt;
&lt;h2&gt;一條適合 Ubuntu production 的落地路線&lt;/h2&gt;
&lt;p&gt;以下不是可以直接複製到任何主機的萬用 script，而是一條變更順序。套件庫與支援版本會變動，實際安裝前仍要以 &lt;a href=&quot;https://docs.docker.com/engine/install/ubuntu/&quot;&gt;Docker 官方 Ubuntu 文件&lt;/a&gt; 為準。&lt;/p&gt;
&lt;h3&gt;第一步：選定 account 與 migration 策略&lt;/h3&gt;
&lt;p&gt;先指定一個 non-root account，例如 &lt;code&gt;deployer&lt;/code&gt;。如果現有帳號已符合權限隔離目標，可以直接沿用；建立新帳號、安裝 SSH key 或移除 sudo 都是獨立的 access change，不能和 Docker 安裝一起默默完成。&lt;/p&gt;
&lt;p&gt;接著明確選擇：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Fresh setup&lt;/strong&gt;：主機沒有既有 Docker workload。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Coexistence&lt;/strong&gt;：rootful 與 rootless daemon 暫時並存，靠 Docker context 明確選擇。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Migration&lt;/strong&gt;：盤點並停妥既有 workload 後，停用 rootful &lt;code&gt;docker.service&lt;/code&gt; 與 &lt;code&gt;docker.socket&lt;/code&gt;。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Migration 前至少要從 administrator context 盤點：&lt;/p&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token function&quot;&gt;sudo&lt;/span&gt; &lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; &lt;span class=&quot;token function&quot;&gt;ps&lt;/span&gt; &lt;span class=&quot;token parameter variable&quot;&gt;-a&lt;/span&gt;
&lt;span class=&quot;token function&quot;&gt;sudo&lt;/span&gt; &lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; image &lt;span class=&quot;token function&quot;&gt;ls&lt;/span&gt;
&lt;span class=&quot;token function&quot;&gt;sudo&lt;/span&gt; &lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; volume &lt;span class=&quot;token function&quot;&gt;ls&lt;/span&gt;
&lt;span class=&quot;token function&quot;&gt;sudo&lt;/span&gt; &lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; network &lt;span class=&quot;token function&quot;&gt;ls&lt;/span&gt;
&lt;span class=&quot;token function&quot;&gt;sudo&lt;/span&gt; systemctl list-dependencies &lt;span class=&quot;token parameter variable&quot;&gt;--reverse&lt;/span&gt; docker.service
&lt;span class=&quot;token function&quot;&gt;sudo&lt;/span&gt; ss &lt;span class=&quot;token parameter variable&quot;&gt;-lntup&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;這些是 read-only inventory，但因為會使用 &lt;code&gt;sudo&lt;/code&gt;，仍應讓主機擁有者先看到命令與目的。還要搜尋 deployment scripts、CI 與其他 system services 是否寫死 &lt;code&gt;/var/run/docker.sock&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;不要把 rootful &lt;code&gt;/var/lib/docker&lt;/code&gt; 直接搬進 &lt;code&gt;~/.local/share/docker&lt;/code&gt;，也不要在「migration」名義下刪除舊資料。Image 可以重拉，volume 與 database 則需要 workload-specific 的備份和搬遷方案。&lt;/p&gt;
&lt;h3&gt;第二步：跑 read-only preflight&lt;/h3&gt;
&lt;p&gt;Skill 內附的 &lt;code&gt;preflight.sh&lt;/code&gt; 會要求明確 target user：&lt;/p&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token function&quot;&gt;bash&lt;/span&gt; scripts/preflight.sh &lt;span class=&quot;token parameter variable&quot;&gt;--user&lt;/span&gt; deployer&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;輸出使用 &lt;code&gt;check.&amp;lt;name&amp;gt;=&amp;lt;status&amp;gt;:&amp;lt;detail&amp;gt;&lt;/code&gt; 格式，最後會得到：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;overall=ready&lt;/code&gt;：基礎條件齊全；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;overall=needs-attention&lt;/code&gt;：有缺少套件、subordinate IDs、cgroup 或衝突狀態；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;overall=unsupported&lt;/code&gt;：host class 不在 skill 支援範圍。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;它會檢查 distribution、systemd、target account、&lt;code&gt;newuidmap&lt;/code&gt;、&lt;code&gt;newgidmap&lt;/code&gt;、subordinate IDs、rootful units 與 socket、rootless context、user socket、linger、cgroup v2、AppArmor/SELinux、firewall tools 與 low-port policy。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;needs-attention&lt;/code&gt; 不是「可以加 &lt;code&gt;--force&lt;/code&gt; 跳過」，而是 change plan 的輸入。每個 attention 都要先判斷它是安裝前提、workload-specific 限制，還是刻意保留的 coexistence 狀態。&lt;/p&gt;
&lt;h3&gt;第三步：準備官方套件與 rootless prerequisites&lt;/h3&gt;
&lt;p&gt;Ubuntu 通常需要 &lt;code&gt;uidmap&lt;/code&gt;、&lt;code&gt;dbus-user-session&lt;/code&gt;、&lt;code&gt;slirp4netns&lt;/code&gt;、&lt;code&gt;fuse-overlayfs&lt;/code&gt;，以及 Docker 官方套件中的 &lt;code&gt;docker-ce-rootless-extras&lt;/code&gt;。後者提供 &lt;code&gt;dockerd-rootless-setuptool.sh&lt;/code&gt; 與所需整合。&lt;/p&gt;
&lt;p&gt;新增 apt repository、安裝或移除套件都會修改 host，必須先展示：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;完整命令與 repository URL；&lt;/li&gt;
&lt;li&gt;會新增或移除哪些 packages；&lt;/li&gt;
&lt;li&gt;是否可能自動啟動 rootful Docker；&lt;/li&gt;
&lt;li&gt;失敗與 rollback 方式。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;套件安裝完成後要立刻檢查 &lt;code&gt;docker.service&lt;/code&gt; 和 &lt;code&gt;docker.socket&lt;/code&gt;。Ubuntu 的 Docker packages 可能啟動 rootful system service；這不代表應該馬上停掉它，而是要回到前面核准過的 coexistence 或 migration 決策。&lt;/p&gt;
&lt;p&gt;也不要為了方便改用 &lt;code&gt;curl ... | sh&lt;/code&gt;。Skill 優先採用 Docker 的 signed distribution packages；只有 packages 路徑不可行時，才下載官方 standalone installer 到具名檔案，讓使用者檢查後另行批准。&lt;/p&gt;
&lt;p&gt;Ubuntu、Debian 與 Fedora 的整體流程相同，但不要混用 package commands：&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Distribution&lt;/th&gt;
&lt;th&gt;Rootless prerequisites 重點&lt;/th&gt;
&lt;th&gt;安裝前特別確認&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Ubuntu&lt;/td&gt;
&lt;td&gt;&lt;code&gt;uidmap&lt;/code&gt;、&lt;code&gt;dbus-user-session&lt;/code&gt;、&lt;code&gt;slirp4netns&lt;/code&gt;、&lt;code&gt;fuse-overlayfs&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;新版 Ubuntu 的 unprivileged user namespace 與 AppArmor 狀態&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Debian&lt;/td&gt;
&lt;td&gt;與 Ubuntu 類似，使用 Debian 專屬 Docker repository&lt;/td&gt;
&lt;td&gt;Derivative distribution 不能自行假設能沿用 Debian codename&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Fedora&lt;/td&gt;
&lt;td&gt;&lt;code&gt;newuidmap&lt;/code&gt;、&lt;code&gt;newgidmap&lt;/code&gt; 通常由 &lt;code&gt;shadow-utils&lt;/code&gt; 提供，另檢查 &lt;code&gt;slirp4netns&lt;/code&gt;、&lt;code&gt;fuse-overlayfs&lt;/code&gt;、&lt;code&gt;iptables&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;DNF command 版本、repository signing key 與 SELinux 狀態&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;實際命令應分別回到 Docker 官方的 &lt;a href=&quot;https://docs.docker.com/engine/install/ubuntu/&quot;&gt;Ubuntu&lt;/a&gt;、&lt;a href=&quot;https://docs.docker.com/engine/install/debian/&quot;&gt;Debian&lt;/a&gt; 或 &lt;a href=&quot;https://docs.docker.com/engine/install/fedora/&quot;&gt;Fedora&lt;/a&gt; 文件核對。Skill 也只會在 preflight 確認 &lt;code&gt;ID&lt;/code&gt; 後載入對應 reference，不會把 Debian derivative 或其他 RPM distribution 當成等價環境。&lt;/p&gt;
&lt;h3&gt;第四步：用 target user 的真實 login session 安裝&lt;/h3&gt;
&lt;p&gt;Rootless service 不是一個寫了 &lt;code&gt;User=deployer&lt;/code&gt; 的 system-wide service。Docker 官方的 &lt;a href=&quot;https://docs.docker.com/engine/security/rootless/tips/&quot;&gt;Rootless tips&lt;/a&gt; 明確說明，這種模式不受支援。&lt;/p&gt;
&lt;p&gt;應透過 SSH 或真正的 login session 進入 target user，先確認環境，再執行 setup：&lt;/p&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token function&quot;&gt;whoami&lt;/span&gt;
&lt;span class=&quot;token function&quot;&gt;id&lt;/span&gt; &lt;span class=&quot;token parameter variable&quot;&gt;-u&lt;/span&gt;
&lt;span class=&quot;token builtin class-name&quot;&gt;printf&lt;/span&gt; &lt;span class=&quot;token string&quot;&gt;&#39;HOME=%s&#92;nXDG_RUNTIME_DIR=%s&#92;n&#39;&lt;/span&gt; &lt;span class=&quot;token string&quot;&gt;&quot;&lt;span class=&quot;token environment constant&quot;&gt;$HOME&lt;/span&gt;&quot;&lt;/span&gt; &lt;span class=&quot;token string&quot;&gt;&quot;&lt;span class=&quot;token environment constant&quot;&gt;$XDG_RUNTIME_DIR&lt;/span&gt;&quot;&lt;/span&gt;
dockerd-rootless-setuptool.sh check
dockerd-rootless-setuptool.sh &lt;span class=&quot;token function&quot;&gt;install&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;正常情況下，setup tool 會建立 &lt;code&gt;rootless&lt;/code&gt; Docker context 與 user service。優先使用 context：&lt;/p&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; context use rootless
&lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; context inspect rootless&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;只有不支援 context 的程式才設定：&lt;/p&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token builtin class-name&quot;&gt;export&lt;/span&gt; &lt;span class=&quot;token assign-left variable&quot;&gt;DOCKER_HOST&lt;/span&gt;&lt;span class=&quot;token operator&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;token string&quot;&gt;&quot;unix://&lt;span class=&quot;token environment constant&quot;&gt;$XDG_RUNTIME_DIR&lt;/span&gt;/docker.sock&quot;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;不要把 &lt;code&gt;DOCKER_HOST&lt;/code&gt; 無條件寫進全域 shell 設定，否則它會覆蓋 context selection，之後在 rootful/rootless coexistence 或操作其他 remote context 時很容易連錯 daemon。&lt;/p&gt;
&lt;h3&gt;第五步：讓 service 跨過 logout 與 reboot&lt;/h3&gt;
&lt;p&gt;Production service 不能在 &lt;code&gt;deployer&lt;/code&gt; 登出後跟著消失。先說明 user lingering 會讓該帳號的 user manager 在沒有登入 session 時持續存在，取得核准後，由 administrator 執行：&lt;/p&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token function&quot;&gt;sudo&lt;/span&gt; loginctl enable-linger deployer&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;再由 &lt;code&gt;deployer&lt;/code&gt; 執行：&lt;/p&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;systemctl &lt;span class=&quot;token parameter variable&quot;&gt;--user&lt;/span&gt; &lt;span class=&quot;token builtin class-name&quot;&gt;enable&lt;/span&gt; &lt;span class=&quot;token parameter variable&quot;&gt;--now&lt;/span&gt; docker.service&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;這兩個動作分屬 host policy 與 user service lifecycle，不要因為常被寫在同一段教學裡，就當成同一個無須說明的步驟。&lt;/p&gt;
&lt;h3&gt;第六步：處理 80/443 與 firewall&lt;/h3&gt;
&lt;p&gt;Rootless process 預設不能直接綁定 1024 以下的 privileged ports。最單純的設計是讓 container 發布 8080/8443，再由 host reverse proxy 或外部 load balancer 接 80/443。&lt;/p&gt;
&lt;p&gt;如果 rootless daemon 必須直接發布 80/443，Docker 文件提供兩類做法：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;調低 &lt;code&gt;net.ipv4.ip_unprivileged_port_start&lt;/code&gt;：影響 host 上所有 unprivileged processes，範圍較大。&lt;/li&gt;
&lt;li&gt;對實際的 RootlessKit binary 設定 &lt;code&gt;CAP_NET_BIND_SERVICE&lt;/code&gt;：範圍較窄，但 package upgrade 後可能需要重新檢查。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;兩者都不是 base installation 的附帶步驟。要先確認需求、顯示 persistent config 或 binary path、取得 approval，再修改 sysctl 或 capability。&lt;/p&gt;
&lt;p&gt;Firewall 也一樣。偵測到 &lt;code&gt;ufw&lt;/code&gt;、&lt;code&gt;firewalld&lt;/code&gt;、&lt;code&gt;nft&lt;/code&gt; 或 &lt;code&gt;iptables&lt;/code&gt;，不等於它正在管理這台主機，更不代表可以自動開放 80/443。應先讀取現有 policy，只新增實際需要、來源範圍最小的規則，最後從外部 client 驗證。&lt;/p&gt;
&lt;h2&gt;Installer 顯示成功，還不算完成&lt;/h2&gt;
&lt;p&gt;正式環境最常見的錯誤，是看到 &lt;code&gt;dockerd-rootless-setuptool.sh install&lt;/code&gt; exit code 0 就宣布完成。&lt;/p&gt;
&lt;p&gt;至少要在 target user 的 login session 驗證：&lt;/p&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; context show
&lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; info
&lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; compose version
systemctl &lt;span class=&quot;token parameter variable&quot;&gt;--user&lt;/span&gt; is-enabled docker.service
systemctl &lt;span class=&quot;token parameter variable&quot;&gt;--user&lt;/span&gt; is-active docker.service
loginctl show-user &lt;span class=&quot;token string&quot;&gt;&quot;&lt;span class=&quot;token variable&quot;&gt;&lt;span class=&quot;token variable&quot;&gt;$(&lt;/span&gt;&lt;span class=&quot;token function&quot;&gt;id&lt;/span&gt; &lt;span class=&quot;token parameter variable&quot;&gt;-un&lt;/span&gt;&lt;span class=&quot;token variable&quot;&gt;)&lt;/span&gt;&lt;/span&gt;&quot;&lt;/span&gt; &lt;span class=&quot;token parameter variable&quot;&gt;-p&lt;/span&gt; Linger
pgrep &lt;span class=&quot;token parameter variable&quot;&gt;-u&lt;/span&gt; &lt;span class=&quot;token string&quot;&gt;&quot;&lt;span class=&quot;token variable&quot;&gt;&lt;span class=&quot;token variable&quot;&gt;$(&lt;/span&gt;&lt;span class=&quot;token function&quot;&gt;id&lt;/span&gt; &lt;span class=&quot;token parameter variable&quot;&gt;-u&lt;/span&gt;&lt;span class=&quot;token variable&quot;&gt;)&lt;/span&gt;&lt;/span&gt;&quot;&lt;/span&gt; &lt;span class=&quot;token parameter variable&quot;&gt;-af&lt;/span&gt; &lt;span class=&quot;token string&quot;&gt;&#39;dockerd|rootlesskit&#39;&lt;/span&gt;
&lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; run &lt;span class=&quot;token parameter variable&quot;&gt;--rm&lt;/span&gt; hello-world&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;重點不是每個 command 都「有輸出」，而是證據彼此一致：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;active context 指向 rootless user socket；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;docker info&lt;/code&gt; 的 Security Options 包含 &lt;code&gt;rootless&lt;/code&gt;；&lt;/li&gt;
&lt;li&gt;daemon process 屬於 target user；&lt;/li&gt;
&lt;li&gt;Compose 可用；&lt;/li&gt;
&lt;li&gt;user service 已 enable 且 active；&lt;/li&gt;
&lt;li&gt;linger 符合 reboot 後啟動需求；&lt;/li&gt;
&lt;li&gt;實際 container 能執行；&lt;/li&gt;
&lt;li&gt;對外 port 能通過預期的 firewall path；&lt;/li&gt;
&lt;li&gt;rootful daemon 是刻意停用，或刻意 coexist。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;最好再做一次真實的 login boundary 或 reboot 驗證。否則你測到的可能只是當前 shell 留下來的環境，不是能在主機重啟後自動恢復的 production service。&lt;/p&gt;
&lt;h2&gt;Rootless 的代價與不適用情境&lt;/h2&gt;
&lt;p&gt;Rootless 是更小的 host privilege，不是免費且透明的相容模式。&lt;/p&gt;
&lt;h3&gt;cgroup resource limits 可能沒有完整生效&lt;/h3&gt;
&lt;p&gt;&lt;a href=&quot;https://docs.docker.com/engine/security/rootless/tips/&quot;&gt;Docker 官方的 Rootless tips&lt;/a&gt;說明，rootless mode 的 &lt;code&gt;--cpus&lt;/code&gt;、&lt;code&gt;--memory&lt;/code&gt;、&lt;code&gt;--pids-limit&lt;/code&gt; 等 cgroup flags，需要 cgroup v2 與 systemd。即使條件成立，預設也可能只 delegation 部分 controllers。&lt;/p&gt;
&lt;p&gt;因此 &lt;code&gt;docker compose.yml&lt;/code&gt; 寫了 limit 不等於 kernel 真的 enforcement。需要 CPU、cpuset、I/O、memory 或 pids 控制的 workload，要檢查 &lt;code&gt;docker info&lt;/code&gt;、可用 controllers 與 user service delegation。Docker 若警告某個 limit 被忽略，就不能把它列為已完成的 production guardrail。&lt;/p&gt;
&lt;h3&gt;Bind mount 會遇到 UID/GID mapping&lt;/h3&gt;
&lt;p&gt;Container 內的 UID/GID 映射到 subordinate range 後，既有 host directory 的 ownership 可能不再符合預期。修正時應針對 workload 真正使用的窄路徑處理，不要為了讓 container 寫入，對大範圍目錄遞迴 &lt;code&gt;chown&lt;/code&gt; 或開放權限。&lt;/p&gt;
&lt;h3&gt;特殊網路、device 與 orchestration 要另外評估&lt;/h3&gt;
&lt;p&gt;Rootless networking、privileged ports、host firewall、device access 和嚴格 resource controls 都可能需要額外整合。Docker Desktop、沒有適當 systemd user session 的 WSL、non-systemd 或 immutable hosts，也不適合直接照這篇的 service 流程操作。&lt;/p&gt;
&lt;p&gt;Kubernetes 與多節點 orchestration 更不是「把每台 host 改成 Rootless Docker」就完成。它們有自己的 runtime、node privilege、networking、storage 與 workload security model，應另做平台層級的設計。&lt;/p&gt;
&lt;h3&gt;Rootless 也不會阻止 target user 傷害自己的資料&lt;/h3&gt;
&lt;p&gt;如果 &lt;code&gt;deployer&lt;/code&gt; 本來就能寫入 &lt;code&gt;/srv/my-app/data&lt;/code&gt;，那麼由它擁有的 rootless containers 也可能透過核准的 bind mount 修改這些資料。攻擊者拿到 rootless socket 後，仍能操作這個 daemon 及其可存取資源。&lt;/p&gt;
&lt;p&gt;所以最精準的說法不是「Rootless Docker 讓 container 變安全」，而是：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;Rootless Docker 把 Docker daemon 與 containers 從 host root 權限移開，讓一層安全控制失敗時，不必直接等同整台主機最高權限失守。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2&gt;一張 production 決策表&lt;/h2&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;情境&lt;/th&gt;
&lt;th&gt;建議&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;單機 Linux VM、Docker Compose、一般 Web/API/DB&lt;/td&gt;
&lt;td&gt;優先評估 rootless，通常是很好的預設&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;可由 reverse proxy 接 80/443&lt;/td&gt;
&lt;td&gt;Rootless 很適合，container 使用高 host port&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;既有 rootful workload 正在服務&lt;/td&gt;
&lt;td&gt;先 inventory，選 coexistence 或分階段 migration&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;CI 或 dashboard 需要 Docker socket&lt;/td&gt;
&lt;td&gt;仍視為 daemon admin；限制 account、socket 與可掛載資料&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;嚴格 CPU/I/O limits&lt;/td&gt;
&lt;td&gt;先驗證 cgroup v2 與 controller delegation&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;大量既有 bind mounts&lt;/td&gt;
&lt;td&gt;先測 UID/GID mapping 與 ownership&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;需要 host devices、特殊 networking 或 privileged workload&lt;/td&gt;
&lt;td&gt;不要強套，改做 workload-specific security review&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Kubernetes、多節點或非 systemd host&lt;/td&gt;
&lt;td&gt;不套用本文流程，使用平台專屬設計&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;h2&gt;收尾：把 rootless 當成預設問題，而不是預設答案&lt;/h2&gt;
&lt;p&gt;正式環境要問的，不應只是「Docker 有沒有跑起來」，而是「這個 daemon 為什麼需要 host root？」&lt;/p&gt;
&lt;p&gt;如果答案只是因為安裝教學一直這樣做，那就值得跑一次 read-only audit，盤點實際需求。對一般的單機 Linux 與 Docker Compose 部署，Rootless Docker 往往能用可接受的整合成本，換到更清楚的 account boundary 與更小的 host blast radius。&lt;/p&gt;
&lt;p&gt;但別跳過限制，也別把 migration 當成一條停服務的指令。先 inventory、把 privileged changes 分組、逐項確認 rollback，最後用 context、socket、process owner、service lifecycle 與真實 workload 證明它能在 production 運作。&lt;/p&gt;
&lt;p&gt;這正是 &lt;code&gt;skill-rootless-docker&lt;/code&gt; 想固定下來的流程：不是讓 agent 更快取得 root，而是讓它知道什麼時候不該碰 root。&lt;/p&gt;
&lt;h2&gt;參考資料&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.docker.com/engine/security/rootless/&quot;&gt;Docker Docs — Rootless mode&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.docker.com/engine/security/rootless/tips/&quot;&gt;Docker Docs — Rootless mode tips&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.docker.com/engine/security/rootless/troubleshoot/&quot;&gt;Docker Docs — Rootless mode troubleshooting&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.docker.com/engine/security/&quot;&gt;Docker Docs — Docker Engine security&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.docker.com/engine/install/linux-postinstall/&quot;&gt;Docker Docs — Linux post-installation steps&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.docker.com/engine/security/userns-remap/&quot;&gt;Docker Docs — Isolate containers with a user namespace&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.docker.com/engine/install/ubuntu/&quot;&gt;Docker Docs — Install Docker Engine on Ubuntu&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/FWcloud916/skill-rootless-docker&quot;&gt;skill-rootless-docker — GitHub repository&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</content>
  </entry>
  
  <entry>
    <title>一個 Meta App 搞不定三平台：FB / IG / Threads 申請實戰踩坑記</title>
    <link href="https://imfw.io/posts/2026/2026-07-20-meta-app-setup-pitfalls/"/>
    <updated>2026-07-20T00:00:00Z</updated>
    <id>https://imfw.io/posts/2026/2026-07-20-meta-app-setup-pitfalls/</id>
    <content type="html">&lt;p&gt;&lt;a href=&quot;https://imfw.io/posts/2026/2026-07-13-social-platform-apis-2026/&quot;&gt;上一篇&lt;/a&gt;整理了 2026 年社群平台 API 的現況
這篇記錄實際到 developers.facebook.com 把 App 辦下來的過程&lt;/p&gt;
&lt;p&gt;動手前我以為「一個 Meta App 搞定 FB / IG / Threads 三平台」
動手後發現 : 是&lt;strong&gt;兩個 App、三組憑證&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;以下是完整流程與每一個坑&lt;/p&gt;
&lt;h2&gt;先攤開地圖：兩個 App、三組憑證&lt;/h2&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;平台&lt;/th&gt;
&lt;th&gt;掛在哪個 App&lt;/th&gt;
&lt;th&gt;憑證&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Facebook&lt;/td&gt;
&lt;td&gt;App A ( Use case 選 Other → Business )&lt;/td&gt;
&lt;td&gt;App A 的 App ID / Secret&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Instagram&lt;/td&gt;
&lt;td&gt;App A 加 Instagram product&lt;/td&gt;
&lt;td&gt;&lt;strong&gt;另一組&lt;/strong&gt; Instagram App ID / Secret&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Threads&lt;/td&gt;
&lt;td&gt;&lt;strong&gt;App B&lt;/strong&gt; ( Use case 選 Access the Threads API )&lt;/td&gt;
&lt;td&gt;App B 的 App ID / Secret&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;為什麼會長這樣 ? 兩個坑造成的，一個一個講&lt;/p&gt;
&lt;h2&gt;坑一：Threads 必須另建一個獨立 App&lt;/h2&gt;
&lt;p&gt;新版的 Create App 流程裡，&lt;strong&gt;Use case 決定一切&lt;/strong&gt; — 它決定這個 App 之後能加哪些 API 、權限與產品&lt;/p&gt;
&lt;p&gt;問題來了 : 用 Other → Business 建立的 App ( FB / IG 用的那種 )
事後&lt;strong&gt;加不進&lt;/strong&gt; Threads use case，dashboard 裡根本沒有這個選項&lt;/p&gt;
&lt;p&gt;所以流程只能是 :&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;My Apps → &lt;strong&gt;Create App&lt;/strong&gt; ( 不能沿用 FB / IG 那個 )&lt;/li&gt;
&lt;li&gt;Use case 選 &lt;strong&gt;Access the Threads API&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;在 use case 設定裡勾權限 : &lt;code&gt;threads_basic&lt;/code&gt; ( 必要 ) + &lt;code&gt;threads_content_publish&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;App settings → Basic 拿 App ID / App Secret&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Threads 跟 IG 本來就是完全獨立的產品，token 、 Secret 、權限都分開
現在連 App 都要分開，心理上就當它們是不同公司吧&lt;/p&gt;
&lt;h2&gt;坑二：IG 會偷偷給你第二組 App ID / Secret&lt;/h2&gt;
&lt;p&gt;Instagram 用新的「 Instagram API with Instagram Login 」，好處是&lt;strong&gt;不用綁 FB 粉專&lt;/strong&gt;了
IG 帳號轉成商業或創作者帳號 ( 免費、可逆 ) 就能直接授權&lt;/p&gt;
&lt;p&gt;設定流程 :&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;App dashboard → Add Product → &lt;strong&gt;Instagram&lt;/strong&gt; → 選 &lt;strong&gt;Instagram API with Instagram Login&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;到「 API setup with Instagram business login 」頁面設定&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;坑就在第 2 步的頁面裡 —
這個 product 有&lt;strong&gt;自己專屬的 Instagram App ID / App Secret&lt;/strong&gt;
跟 App settings → Basic 那組&lt;strong&gt;不是同一組&lt;/strong&gt;!!&lt;/p&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token comment&quot;&gt;# .env — 同一個 App 卻有兩組憑證&lt;/span&gt;
&lt;span class=&quot;token assign-left variable&quot;&gt;META_APP_ID&lt;/span&gt;&lt;span class=&quot;token operator&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;token punctuation&quot;&gt;..&lt;/span&gt;.        &lt;span class=&quot;token comment&quot;&gt;# App settings → Basic ( FB 用 )&lt;/span&gt;
&lt;span class=&quot;token assign-left variable&quot;&gt;META_APP_SECRET&lt;/span&gt;&lt;span class=&quot;token operator&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;token punctuation&quot;&gt;..&lt;/span&gt;.
&lt;span class=&quot;token assign-left variable&quot;&gt;IG_APP_ID&lt;/span&gt;&lt;span class=&quot;token operator&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;token punctuation&quot;&gt;..&lt;/span&gt;.          &lt;span class=&quot;token comment&quot;&gt;# Instagram product 頁面 ( IG 用 )&lt;/span&gt;
&lt;span class=&quot;token assign-left variable&quot;&gt;IG_APP_SECRET&lt;/span&gt;&lt;span class=&quot;token operator&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;token punctuation&quot;&gt;..&lt;/span&gt;.&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;拿錯組去跑 OAuth，錯誤訊息不會告訴你「你拿錯組了」，只會讓你懷疑人生
發 IG 的另外兩個限制順便記著 : 圖片&lt;strong&gt;只收 JPEG&lt;/strong&gt; 且必須放在&lt;strong&gt;公開 URL&lt;/strong&gt; ( Meta 伺服器會自己來抓 )&lt;/p&gt;
&lt;h2&gt;Facebook：坑最少，但要先有粉專&lt;/h2&gt;
&lt;p&gt;FB 反而是三個裡面最單純的，條件只有一個 :
&lt;strong&gt;個人檔案不能用 API 發文&lt;/strong&gt;，要有一個你當管理員的粉絲專頁&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;App dashboard → Add Product → &lt;strong&gt;Facebook Login for Business&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;到 &lt;a href=&quot;https://developers.facebook.com/tools/explorer&quot;&gt;Graph API Explorer&lt;/a&gt; 產生 User token，勾三個權限 :
&lt;code&gt;pages_manage_posts&lt;/code&gt; + &lt;code&gt;pages_read_engagement&lt;/code&gt; + &lt;code&gt;pages_show_list&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;用它換&lt;strong&gt;長效 Page token&lt;/strong&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;換到的 Page token 在 dev mode 、權限不變的情況下&lt;strong&gt;幾乎不過期&lt;/strong&gt;
辦一次就可以忘記它，三平台裡的模範生&lt;/p&gt;
&lt;h2&gt;Tester 邀請：發出去，還要記得去「接受」&lt;/h2&gt;
&lt;p&gt;IG 跟 Threads 都要把自己的帳號加成 Tester，而且是&lt;strong&gt;兩段式&lt;/strong&gt;的 :
dashboard 發邀請是一段，到手機 App 裡接受邀請是另一段&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Instagram&lt;/strong&gt; : product 頁加 Instagram Tester → 打開 IG App 接受邀請&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Threads&lt;/strong&gt; : App roles → Roles 加 Threads Tester → 打開 Threads App
( 設定 → 帳號 → 網站權限 → 邀請 ) 接受&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;第二段超容易忘
忘了的症狀是 : token 拿得到，API 打下去卻一直吐權限錯誤，讓你回頭檢查程式碼半天&lt;/p&gt;
&lt;h2&gt;Token 生命週期：三個平台三種個性&lt;/h2&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;平台&lt;/th&gt;
&lt;th&gt;效期&lt;/th&gt;
&lt;th&gt;維護&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Facebook&lt;/td&gt;
&lt;td&gt;幾乎不過期&lt;/td&gt;
&lt;td&gt;不用管&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Instagram&lt;/td&gt;
&lt;td&gt;60 天&lt;/td&gt;
&lt;td&gt;過期前刷新 ; &lt;strong&gt;過期就不能刷&lt;/strong&gt;，只能重新授權&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Threads&lt;/td&gt;
&lt;td&gt;60 天&lt;/td&gt;
&lt;td&gt;同上，而且 token &lt;strong&gt;存在滿 24 小時後才能刷新&lt;/strong&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;Threads 那條「滿 24 小時才能刷」是文件裡一行小字
如果你授權完馬上測試刷新流程，會拿到失敗然後開始 debug 一個不存在的 bug ( 我 )&lt;/p&gt;
&lt;p&gt;60 天的過期週期剛好長到讓人忘記它的存在
建議直接排一個&lt;strong&gt;每月一次&lt;/strong&gt;的排程把 IG 跟 Threads 一起刷掉，一勞永逸&lt;/p&gt;
&lt;h2&gt;收尾：dev mode 就是完全體&lt;/h2&gt;
&lt;p&gt;整趟辦下來，最重要的還是這條共通規則 :&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;App 維持 Development mode 、自己的帳號持有 app role
對「自己的資產」發文&lt;strong&gt;不需要 App Review，完全免費&lt;/strong&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;不用錄影、不用寫使用情境、不用等審核
兩個 App 、三組憑證、幾封 Tester 邀請，換來三個平台的免費自動發文
比起隔壁按次計費的 X，這些 dashboard 上的小坑真的只是小坑啦&lt;/p&gt;
&lt;h2&gt;參考資料&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://developers.facebook.com/docs/development/create-an-app/threads-use-case/&quot;&gt;Threads Use Case — App Development with Meta&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://developers.facebook.com/docs/threads/get-started/create-an-app/&quot;&gt;Threads API — Create an App&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://developers.facebook.com/docs/threads/get-started/get-access-tokens-and-permissions/&quot;&gt;Threads API — Access Tokens and Permissions&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://developers.facebook.com/docs/instagram-platform/create-an-instagram-app/&quot;&gt;Instagram Platform — Create an Instagram App&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://developers.facebook.com/docs/instagram-platform/instagram-api-with-instagram-login/business-login/&quot;&gt;Business Login for Instagram&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://developers.facebook.com/docs/pages-api/getting-started/&quot;&gt;Meta Pages API — Get Started&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</content>
  </entry>
  
  <entry>
    <title>Docker 基礎學習路線：從 Container、Image 到 Dockerfile</title>
    <link href="https://imfw.io/posts/2026/2026-07-13-docker-basics-guide/"/>
    <updated>2026-07-13T00:00:00Z</updated>
    <id>https://imfw.io/posts/2026/2026-07-13-docker-basics-guide/</id>
    <content type="html">&lt;p&gt;學 Docker 最有效的順序是：先理解 container 是隔離的 process，再理解 image 如何提供執行所需檔案，最後用 Dockerfile 建立自己的 image。不要一開始就背所有 CLI；先完成「run 現成 image → 觀察 container → build 自己的 image」這個循環。&lt;/p&gt;
&lt;h2&gt;適合誰&lt;/h2&gt;
&lt;p&gt;這條路線適合第一次接觸 Docker、想統一團隊開發環境，或需要把 Web 應用程式放進 container 的開發者。開始前只需要能使用 terminal，不要求 Kubernetes 或雲端平台知識。&lt;/p&gt;
&lt;h2&gt;核心關係&lt;/h2&gt;
&lt;pre class=&quot;language-text&quot;&gt;&lt;code class=&quot;language-text&quot;&gt;Dockerfile ──build──&gt; Image ──run──&gt; Container
                         │
                         └──push／pull──&gt; Registry&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Dockerfile&lt;/strong&gt; 保存建置步驟。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Image&lt;/strong&gt; 是唯讀、分層、可分發的應用套件。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Container&lt;/strong&gt; 是 image 的執行實例。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Registry&lt;/strong&gt; 用來保存與分享 image。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;第一步：理解 Container 為什麼存在&lt;/h2&gt;
&lt;p&gt;先讀 &lt;a href=&quot;https://imfw.io/posts/2020/2020-02-16-docker-basic-concept/&quot;&gt;Docker 基本概念&lt;/a&gt;，理解 container 與 VM 的隔離方式和使用情境。接著用 &lt;a href=&quot;https://imfw.io/posts/2020/2020-02-22-docker-basic-container/&quot;&gt;Docker Container 基本概念&lt;/a&gt;啟動 nginx，練習：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;docker container run&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;port publishing&lt;/li&gt;
&lt;li&gt;bind mount&lt;/li&gt;
&lt;li&gt;查看 log、停止與清理 container&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;完成標準：能解釋為什麼 &lt;code&gt;EXPOSE 80&lt;/code&gt; 不等於 host 可以直接連到 port 80。&lt;/p&gt;
&lt;h2&gt;第二步：理解 Image 與版本&lt;/h2&gt;
&lt;p&gt;閱讀 &lt;a href=&quot;https://imfw.io/posts/2020/2020-03-08-docker-basic-image/&quot;&gt;Docker Image 基本概念&lt;/a&gt;，練習 pull、list、inspect 與 remove。特別確認：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;image 為什麼由 layers 組成&lt;/li&gt;
&lt;li&gt;tag 與 digest 的差異&lt;/li&gt;
&lt;li&gt;為什麼正式部署不應盲目依賴 &lt;code&gt;latest&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;完成標準：能指出目前執行的 container 使用哪個 image tag 或 digest。&lt;/p&gt;
&lt;h2&gt;第三步：建立自己的 Image&lt;/h2&gt;
&lt;p&gt;閱讀 &lt;a href=&quot;https://imfw.io/posts/2020/2020-03-15-docker-basic-dockerfile/&quot;&gt;Dockerfile 基本概念&lt;/a&gt;，用簡單 Node.js 專案完成：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;選擇明確版本的 base image。&lt;/li&gt;
&lt;li&gt;先複製 lockfile 並安裝 dependency。&lt;/li&gt;
&lt;li&gt;再複製應用程式，利用 build cache。&lt;/li&gt;
&lt;li&gt;透過 &lt;code&gt;.dockerignore&lt;/code&gt; 排除 secret 和不必要檔案。&lt;/li&gt;
&lt;li&gt;build image 並以 container 執行。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;完成標準：修改一個程式檔後重新 build，能從輸出辨認 dependency layer 是否重用 cache。&lt;/p&gt;
&lt;h2&gt;建議的下一個主題&lt;/h2&gt;
&lt;p&gt;掌握這四篇後，再依需求學習：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Docker Compose：管理多個互相依賴的服務。&lt;/li&gt;
&lt;li&gt;Named volume：保存需要跨 container 留存的資料。&lt;/li&gt;
&lt;li&gt;Container network：讓服務透過名稱互相連線。&lt;/li&gt;
&lt;li&gt;Multi-stage build：縮小正式 image，避免把建置工具帶進 runtime。&lt;/li&gt;
&lt;li&gt;Healthcheck 與 resource limits：讓部署平台能判斷服務狀態並限制資源。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;官方延伸閱讀&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.docker.com/get-started/&quot;&gt;Docker Get Started&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.docker.com/guides/lab-container-getting-started/&quot;&gt;Docker 官方入門 Lab&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.docker.com/get-started/introduction/whats-next/&quot;&gt;Docker：What&#39;s next&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</content>
  </entry>
  
  <entry>
    <title>2026 年社群平台 API 現況：單人開發者的生存指南</title>
    <link href="https://imfw.io/posts/2026/2026-07-13-social-platform-apis-2026/"/>
    <updated>2026-07-13T00:00:00Z</updated>
    <id>https://imfw.io/posts/2026/2026-07-13-social-platform-apis-2026/</id>
    <content type="html">&lt;p&gt;如果只是替自己的帳號做自動發文，2026 年最容易起步的是 &lt;strong&gt;Threads&lt;/strong&gt;；Facebook 與 Instagram 在 App 維持 Development mode、只操作自己資產時也很實用；X 則要先接受按次計費。真正容易讓自動化失效的地方不是發文 API，而是 Instagram 與 Threads 的 60 天 token，以及 X refresh token 的輪替。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;查核範圍：&lt;/strong&gt; 本文資訊於 2026 年 7 月 13 日依官方文件與實際申請流程整理。平台價格、權限與額度可能調整，實作前請再檢查文中連結的官方文件。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2&gt;30 秒比較&lt;/h2&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;平台&lt;/th&gt;
&lt;th&gt;自動發文成本&lt;/th&gt;
&lt;th&gt;個人工具的主要門檻&lt;/th&gt;
&lt;th&gt;Token 維護&lt;/th&gt;
&lt;th&gt;建議順序&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Threads&lt;/td&gt;
&lt;td&gt;免費&lt;/td&gt;
&lt;td&gt;建立 Meta App，自己的帳號需有 app role&lt;/td&gt;
&lt;td&gt;60 天內刷新&lt;/td&gt;
&lt;td&gt;第一個做&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Facebook&lt;/td&gt;
&lt;td&gt;免費&lt;/td&gt;
&lt;td&gt;只能發粉絲專頁，不能發個人檔案&lt;/td&gt;
&lt;td&gt;長效 Page token&lt;/td&gt;
&lt;td&gt;與 IG 一起做&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Instagram&lt;/td&gt;
&lt;td&gt;免費&lt;/td&gt;
&lt;td&gt;專業帳號；圖片須為公開可抓取的 JPEG&lt;/td&gt;
&lt;td&gt;60 天內刷新&lt;/td&gt;
&lt;td&gt;解決圖片託管後做&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;X&lt;/td&gt;
&lt;td&gt;按次計費&lt;/td&gt;
&lt;td&gt;預先購買 API credits&lt;/td&gt;
&lt;td&gt;refresh token 輪替&lt;/td&gt;
&lt;td&gt;確認受眾後再做&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;這個排序是針對「單人維護、只發布自己的內容」做出的實作建議，不代表各平台對所有產品的通用排名。&lt;/p&gt;
&lt;h2&gt;為什麼重新研究這四套 API&lt;/h2&gt;
&lt;p&gt;我在做部落格的自動發文工具：文章發布後，自動轉發到 X、Facebook、Instagram 和 Threads。結果最花時間的不是寫程式，而是搞懂四個平台目前的申請方式、權限、費用和 token 生命週期。&lt;/p&gt;
&lt;p&gt;以下先列官方規則，再補上我針對單人工具的實作判斷。&lt;/p&gt;
&lt;h2&gt;X：按次計費，含連結的貼文最貴&lt;/h2&gt;
&lt;p&gt;&lt;a href=&quot;https://docs.x.com/x-api/getting-started/pricing&quot;&gt;X 官方定價&lt;/a&gt;採預付 credits 的 pay-per-usage 模式，沒有固定月費。查核當下的公開價格為：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;建立一般內容：&lt;strong&gt;US$0.015／次&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;建立含 URL 的內容：&lt;strong&gt;US$0.20／次&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;讀取 Post：&lt;strong&gt;US$0.005／筆&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;自有帳號資料的 Owned Read：&lt;strong&gt;US$0.001／筆&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;因此，部落格每發布一篇含連結的宣傳貼文，API 成本約為 US$0.20。金額不算高，但如果讀者不在 X 上，就值得先衡量投入效益。&lt;/p&gt;
&lt;h3&gt;X 的授權注意事項&lt;/h3&gt;
&lt;p&gt;發文使用 OAuth 2.0 Authorization Code Flow with PKCE；需要離線存取時，必須要求 &lt;code&gt;offline.access&lt;/code&gt; scope。依 &lt;a href=&quot;https://docs.x.com/fundamentals/authentication/oauth-2-0/authorization-code&quot;&gt;X OAuth 2.0 官方文件&lt;/a&gt;，refresh token 使用後可能輪替，所以刷新 access token 時必須把新的 refresh token 一併回存，不能繼續使用舊值。&lt;/p&gt;
&lt;h2&gt;Meta 三平台：個人工具可先留在 Development mode&lt;/h2&gt;
&lt;p&gt;Facebook、Instagram、Threads 都在 Meta 開發者平台管理。對只操作自己帳號或資產的單人工具，實際申請時可以讓 App 維持 Development mode，並把自己的帳號加入 app role；這樣能先完成自己的發布流程，不必一開始就走提供外部使用者授權所需的 App Review。&lt;/p&gt;
&lt;p&gt;這是開發與自用情境，不等於可以把 Development mode 的 App 當成公開服務。只要開始替其他使用者操作資產，就應重新檢查 App Review、Business Verification 與各權限要求。&lt;/p&gt;
&lt;h3&gt;Facebook：只能發粉絲專頁&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;API 可以發布到自己管理的粉絲專頁，不能替個人 Profile 自動發文。&lt;/li&gt;
&lt;li&gt;常用權限包括 &lt;code&gt;pages_manage_posts&lt;/code&gt;、&lt;code&gt;pages_read_engagement&lt;/code&gt; 與 &lt;code&gt;pages_show_list&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;發文可對 &lt;code&gt;/{page-id}/feed&lt;/code&gt; 傳送 &lt;code&gt;message&lt;/code&gt;，並以 &lt;code&gt;link&lt;/code&gt; 帶入文章網址。&lt;/li&gt;
&lt;li&gt;長效 User token 可交換 Page access token；實際效期仍應以 Access Token Debugger 與 &lt;a href=&quot;https://developers.facebook.com/docs/pages-api/getting-started/&quot;&gt;Pages API 入門文件&lt;/a&gt;顯示為準，不要只靠程式假設永久有效。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Facebook 適合已經有粉絲專頁的人。如果只有個人 Profile，必須先建立專頁才能走這條 API 流程。&lt;/p&gt;
&lt;h3&gt;Instagram：發布流程不再強制綁 Facebook 粉專&lt;/h3&gt;
&lt;p&gt;使用 &lt;a href=&quot;https://developers.facebook.com/docs/instagram-platform/instagram-api-with-instagram-login/&quot;&gt;Instagram API with Instagram Login&lt;/a&gt;時，可以直接以 Instagram 專業帳號授權，不必沿用早期先綁 Facebook 粉專的流程。&lt;/p&gt;
&lt;p&gt;依 &lt;a href=&quot;https://developers.facebook.com/docs/instagram-platform/content-publishing/&quot;&gt;Instagram Content Publishing 官方文件&lt;/a&gt;，自動發布要特別處理：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;圖片必須放在 Meta 伺服器能存取的公開 URL。&lt;/li&gt;
&lt;li&gt;圖片格式與尺寸必須符合端點要求；建立流程前應再次核對官方限制。&lt;/li&gt;
&lt;li&gt;發布分成兩段：先建立 media container，再呼叫 publish。&lt;/li&gt;
&lt;li&gt;API 有內容發布頻率限制，程式應主動查詢使用量，不要只依賴寫死的數字。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;對靜態部落格而言，可以把社群圖片隨網站一起部署，等正式 URL 可公開存取後再請 Instagram 抓取。&lt;/p&gt;
&lt;h3&gt;Threads：純文字最適合驗證流程&lt;/h3&gt;
&lt;p&gt;Threads 支援純文字貼文，不需要先解決 Instagram 的圖片託管問題，因此最適合拿來驗證「建置完成 → 取得文章 URL → 產生貼文 → 發布」這條自動化流程。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;權限通常從 &lt;code&gt;threads_basic&lt;/code&gt; 與 &lt;code&gt;threads_content_publish&lt;/code&gt; 開始。&lt;/li&gt;
&lt;li&gt;支援用 &lt;code&gt;reply_to_id&lt;/code&gt; 回覆自己的貼文，適合做主文加補充串文。&lt;/li&gt;
&lt;li&gt;container 建立與正式 publish 是兩個步驟，程式應處理媒體狀態與重試，而不是只用固定秒數等待。&lt;/li&gt;
&lt;li&gt;額度與回覆規則應以 &lt;a href=&quot;https://developers.facebook.com/docs/threads/posts/&quot;&gt;Threads 發布文件&lt;/a&gt;和 &lt;a href=&quot;https://developers.facebook.com/docs/threads/retrieve-and-manage-replies/create-replies/&quot;&gt;Threads 回覆文件&lt;/a&gt;為準。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Threads 與 Instagram 即使掛在同一個 Meta App 下，token、App Secret 和權限仍是分開管理的兩套資料，不能共用。&lt;/p&gt;
&lt;h2&gt;Token 管理是自動發文最容易壞的地方&lt;/h2&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;平台&lt;/th&gt;
&lt;th&gt;主要生命週期風險&lt;/th&gt;
&lt;th&gt;建議維護方式&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;X&lt;/td&gt;
&lt;td&gt;refresh token 可能輪替&lt;/td&gt;
&lt;td&gt;每次刷新都原子化回存新 access token 與 refresh token&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Facebook&lt;/td&gt;
&lt;td&gt;token 狀態會受角色、密碼與權限變動影響&lt;/td&gt;
&lt;td&gt;定期驗證 token，不把「長效」當成永不失效&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Instagram&lt;/td&gt;
&lt;td&gt;長效 token 需在有效期間內刷新&lt;/td&gt;
&lt;td&gt;排程提早刷新，失敗時告警並保留重新授權流程&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Threads&lt;/td&gt;
&lt;td&gt;與 Instagram 分開的 token 生命週期&lt;/td&gt;
&lt;td&gt;獨立儲存、獨立刷新、獨立告警&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;不要等發文收到 401 才處理。實作時至少要保存 token 到期時間、最後刷新時間與最後一次錯誤，並在失效前主動刷新。&lt;/p&gt;
&lt;h2&gt;給單人開發者的實作順序&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;先接 Threads&lt;/strong&gt;：純文字即可發布，最容易驗證整條自動化。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;再做 Facebook&lt;/strong&gt;：如果已有粉絲專頁，發布流程相對直接。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;處理 Instagram 圖片&lt;/strong&gt;：先確保正式圖片 URL、格式與發布狀態檢查都可靠。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;最後評估 X&lt;/strong&gt;：確認受眾與 US$0.20 含連結貼文成本值得，再購買 credits。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;統一做 token 監控&lt;/strong&gt;：刷新、輪替、失敗告警與重新授權都要在正式自動化前完成。&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;結論&lt;/h2&gt;
&lt;p&gt;2026 年仍然可以用官方 API 為自己的社群帳號做自動發文，但四個平台沒有真正共用的授權或發布抽象。最穩定的做法是共用「文章資料、排程、錯誤處理與觀測」，平台 token 與發布流程則各自封裝。&lt;/p&gt;
&lt;p&gt;如果目標只是替個人部落格省下重複貼文時間，從 Threads 做出最小可行流程，再逐一增加 Facebook、Instagram 和 X，會比一次處理四套權限更容易維護。&lt;/p&gt;
&lt;h2&gt;官方參考資料&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.x.com/x-api/getting-started/pricing&quot;&gt;X API pricing&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.x.com/fundamentals/authentication/oauth-2-0/authorization-code&quot;&gt;X OAuth 2.0 Authorization Code Flow&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://developers.facebook.com/docs/pages-api/getting-started/&quot;&gt;Meta Pages API — Get Started&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://developers.facebook.com/docs/instagram-platform/instagram-api-with-instagram-login/&quot;&gt;Instagram API with Instagram Login&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://developers.facebook.com/docs/instagram-platform/content-publishing/&quot;&gt;Instagram Content Publishing&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://developers.facebook.com/docs/threads/posts/&quot;&gt;Threads API — Publish Content&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://developers.facebook.com/docs/threads/retrieve-and-manage-replies/create-replies/&quot;&gt;Threads API — Create Replies&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</content>
  </entry>
  
  <entry>
    <title>Dockerfile - 基本概念</title>
    <link href="https://imfw.io/posts/2020/2020-03-15-docker-basic-dockerfile/"/>
    <updated>2020-03-15T00:00:00Z</updated>
    <id>https://imfw.io/posts/2020/2020-03-15-docker-basic-dockerfile/</id>
    <content type="html">&lt;p&gt;Dockerfile 是描述如何建立 container image 的文字檔。每個指令都應有明確目的：選擇 base image、安裝 dependency、複製應用程式、設定工作目錄，最後指定 container 啟動時執行的程式。&lt;/p&gt;
&lt;h2&gt;常用指令&lt;/h2&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;指令&lt;/th&gt;
&lt;th&gt;用途&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;FROM&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;指定 base image 並開始新的 build stage&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;WORKDIR&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;設定後續指令的工作目錄&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;COPY&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;從 build context 或其他 stage 複製檔案&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;RUN&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;在 build 階段執行指令並產生 layer&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;CMD&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;提供 container 預設啟動命令或參數&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;ENTRYPOINT&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;指定 container 的主要 executable&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;ENV&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;設定 image 與 container 使用的環境變數&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;ARG&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;設定只在 build 階段使用的變數&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;EXPOSE&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;描述應用程式監聽的 port，不會自動發布 port&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;USER&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;指定後續 build 或 runtime 使用者&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;一般檔案優先用 &lt;code&gt;COPY&lt;/code&gt;；只有確實需要 &lt;code&gt;ADD&lt;/code&gt; 的遠端來源或自動解壓等能力時再使用 &lt;code&gt;ADD&lt;/code&gt;。&lt;/p&gt;
&lt;h2&gt;Node.js 範例&lt;/h2&gt;
&lt;pre class=&quot;language-dockerfile&quot;&gt;&lt;code class=&quot;language-dockerfile&quot;&gt;&lt;span class=&quot;token comment&quot;&gt;# syntax=docker/dockerfile:1&lt;/span&gt;
&lt;span class=&quot;token instruction&quot;&gt;&lt;span class=&quot;token keyword&quot;&gt;FROM&lt;/span&gt; node:22-alpine&lt;/span&gt;

&lt;span class=&quot;token instruction&quot;&gt;&lt;span class=&quot;token keyword&quot;&gt;WORKDIR&lt;/span&gt; /app&lt;/span&gt;

&lt;span class=&quot;token instruction&quot;&gt;&lt;span class=&quot;token keyword&quot;&gt;COPY&lt;/span&gt; package.json package-lock.json ./&lt;/span&gt;
&lt;span class=&quot;token instruction&quot;&gt;&lt;span class=&quot;token keyword&quot;&gt;RUN&lt;/span&gt; npm ci --omit=dev&lt;/span&gt;

&lt;span class=&quot;token instruction&quot;&gt;&lt;span class=&quot;token keyword&quot;&gt;COPY&lt;/span&gt; . .&lt;/span&gt;

&lt;span class=&quot;token instruction&quot;&gt;&lt;span class=&quot;token keyword&quot;&gt;ENV&lt;/span&gt; NODE_ENV=production&lt;/span&gt;
&lt;span class=&quot;token instruction&quot;&gt;&lt;span class=&quot;token keyword&quot;&gt;EXPOSE&lt;/span&gt; 3000&lt;/span&gt;
&lt;span class=&quot;token instruction&quot;&gt;&lt;span class=&quot;token keyword&quot;&gt;CMD&lt;/span&gt; [&lt;span class=&quot;token string&quot;&gt;&quot;npm&quot;&lt;/span&gt;, &lt;span class=&quot;token string&quot;&gt;&quot;start&quot;&lt;/span&gt;]&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;先複製 lockfile 並安裝 dependency，再複製其餘程式碼，可以讓只有應用程式檔案變更時重用 dependency layer 的 build cache。範例中的版本只是示意；實際專案應使用受支援版本並定期更新 base image。&lt;/p&gt;
&lt;p&gt;搭配 &lt;code&gt;.dockerignore&lt;/code&gt;：&lt;/p&gt;
&lt;pre class=&quot;language-text&quot;&gt;&lt;code class=&quot;language-text&quot;&gt;.git
node_modules
npm-debug.log
.env*&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Secret 不應透過 &lt;code&gt;COPY&lt;/code&gt;、&lt;code&gt;ARG&lt;/code&gt; 或 &lt;code&gt;ENV&lt;/code&gt; 永久寫入 image layer；建置或執行時應使用平台提供的 secret 機制。&lt;/p&gt;
&lt;h2&gt;建置與執行&lt;/h2&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; image build &lt;span class=&quot;token parameter variable&quot;&gt;--tag&lt;/span&gt; my-node-app:1.0 &lt;span class=&quot;token builtin class-name&quot;&gt;.&lt;/span&gt;
&lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; container run &lt;span class=&quot;token parameter variable&quot;&gt;--rm&lt;/span&gt; &lt;span class=&quot;token parameter variable&quot;&gt;-p&lt;/span&gt; &lt;span class=&quot;token number&quot;&gt;3000&lt;/span&gt;:3000 my-node-app:1.0&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;EXPOSE 3000&lt;/code&gt; 只提供 metadata；&lt;code&gt;-p 3000:3000&lt;/code&gt; 才把 container port 發布到 host。&lt;/p&gt;
&lt;p&gt;回到 &lt;a href=&quot;https://imfw.io/posts/2026/2026-07-13-docker-basics-guide/&quot;&gt;Docker 基礎學習路線&lt;/a&gt;，或複習 &lt;a href=&quot;https://imfw.io/posts/2020/2020-03-08-docker-basic-image/&quot;&gt;Docker Image 基本概念&lt;/a&gt;。&lt;/p&gt;
&lt;h2&gt;官方參考資料&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.docker.com/reference/dockerfile/&quot;&gt;Dockerfile reference&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.docker.com/get-started/docker-concepts/building-images/writing-a-dockerfile/&quot;&gt;Writing a Dockerfile&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.docker.com/build/building/secrets/&quot;&gt;Build secrets&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</content>
  </entry>
  
  <entry>
    <title>Docker Image - 基本概念</title>
    <link href="https://imfw.io/posts/2020/2020-03-08-docker-basic-image/"/>
    <updated>2020-03-08T00:00:00Z</updated>
    <id>https://imfw.io/posts/2020/2020-03-08-docker-basic-image/</id>
    <content type="html">&lt;p&gt;Docker image 是執行 container 所需檔案、binary、library 與設定的標準化套件。Image 建立後不會直接被修改；內容變更時會產生新的 layer 與新 image。Tag 是方便閱讀的名稱，digest 才是特定內容的不可變識別值。&lt;/p&gt;
&lt;h2&gt;Image 和 container 的關係&lt;/h2&gt;
&lt;p&gt;Image 是可重複使用的套件，container 是它的執行實例。同一個 image 可以啟動多個 container；container runtime 產生的可寫資料不會反向修改原始 image。&lt;/p&gt;
&lt;p&gt;Image 通常由多個 layer 組成。不同 image 可以共用相同 layer，因此下載或儲存時不一定要重複保存全部內容。&lt;/p&gt;
&lt;h2&gt;常用指令&lt;/h2&gt;
&lt;h3&gt;列出本機 image&lt;/h3&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; image &lt;span class=&quot;token function&quot;&gt;ls&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;從 registry 下載&lt;/h3&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; image pull nginx:alpine&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;正式部署應明確指定 tag，對可重現性要求更高時可固定 digest，避免 &lt;code&gt;latest&lt;/code&gt; 指向的內容日後改變。&lt;/p&gt;
&lt;h3&gt;從 Dockerfile 建置&lt;/h3&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; image build &lt;span class=&quot;token parameter variable&quot;&gt;--tag&lt;/span&gt; my-app:1.0 &lt;span class=&quot;token builtin class-name&quot;&gt;.&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;最後的 &lt;code&gt;.&lt;/code&gt; 是 build context；Dockerfile 中的 &lt;code&gt;COPY&lt;/code&gt; 只能讀取 context 內允許的檔案。應搭配 &lt;code&gt;.dockerignore&lt;/code&gt; 排除 &lt;code&gt;.git&lt;/code&gt;、dependency cache、secret 和其他不需要的內容。&lt;/p&gt;
&lt;h3&gt;查看詳細資料與 history&lt;/h3&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; image inspect my-app:1.0
&lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; image &lt;span class=&quot;token function&quot;&gt;history&lt;/span&gt; my-app:1.0&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;移除 image&lt;/h3&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; image &lt;span class=&quot;token function&quot;&gt;rm&lt;/span&gt; my-app:1.0&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;若仍有 container 引用該 image，必須先確認並清理 container，不要在不理解影響時直接強制刪除。&lt;/p&gt;
&lt;h2&gt;Tag 與 digest 怎麼選&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Tag&lt;/strong&gt; 適合人員閱讀與版本管理，例如 &lt;code&gt;my-app:1.0&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Digest&lt;/strong&gt; 對應確切 image 內容，例如 &lt;code&gt;nginx@sha256:...&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;開發流程可以使用有意義的 tag；需要嚴格重現的部署則保存並使用 digest。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;回到 &lt;a href=&quot;https://imfw.io/posts/2026/2026-07-13-docker-basics-guide/&quot;&gt;Docker 基礎學習路線&lt;/a&gt;，或繼續閱讀 &lt;a href=&quot;https://imfw.io/posts/2020/2020-03-15-docker-basic-dockerfile/&quot;&gt;Dockerfile 基本概念&lt;/a&gt;。&lt;/p&gt;
&lt;h2&gt;官方參考資料&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.docker.com/get-started/docker-concepts/the-basics/what-is-an-image/&quot;&gt;Docker：What is an image?&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.docker.com/reference/cli/docker/image/&quot;&gt;docker image CLI&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.docker.com/reference/cli/docker/image/pull/&quot;&gt;docker image pull&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</content>
  </entry>
  
  <entry>
    <title>Docker Container - 基本概念</title>
    <link href="https://imfw.io/posts/2020/2020-02-22-docker-basic-container/"/>
    <updated>2020-02-22T00:00:00Z</updated>
    <id>https://imfw.io/posts/2020/2020-02-22-docker-basic-container/</id>
    <content type="html">&lt;p&gt;Container 是 image 的可執行實例。最常用的起點是 &lt;code&gt;docker run&lt;/code&gt;：它會在本機沒有 image 時先下載，再建立並啟動 container。本篇用 nginx 示範 port publishing、bind mount、背景執行與清理流程。&lt;/p&gt;
&lt;h2&gt;啟動第一個 container&lt;/h2&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; container run &lt;span class=&quot;token parameter variable&quot;&gt;--name&lt;/span&gt; web &lt;span class=&quot;token parameter variable&quot;&gt;-d&lt;/span&gt; &lt;span class=&quot;token parameter variable&quot;&gt;-p&lt;/span&gt; &lt;span class=&quot;token number&quot;&gt;8080&lt;/span&gt;:80 nginx&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;這行指令會：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;以 &lt;code&gt;nginx&lt;/code&gt; image 建立名為 &lt;code&gt;web&lt;/code&gt; 的 container。&lt;/li&gt;
&lt;li&gt;&lt;code&gt;-d&lt;/code&gt;／&lt;code&gt;--detach&lt;/code&gt; 讓它在背景執行。&lt;/li&gt;
&lt;li&gt;&lt;code&gt;-p&lt;/code&gt;／&lt;code&gt;--publish&lt;/code&gt; 將 host 的 &lt;code&gt;8080&lt;/code&gt; 對應到 container 的 &lt;code&gt;80&lt;/code&gt;。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;完成後開啟 &lt;code&gt;http://localhost:8080&lt;/code&gt;，應該會看到 nginx 預設頁面。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;Dockerfile 的 &lt;code&gt;EXPOSE 80&lt;/code&gt; 只是描述服務預期監聽的 port，不會自動把 port 發布到 host；實際對外映射仍要使用 &lt;code&gt;-p&lt;/code&gt; 或其他網路設定。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2&gt;查看狀態與 log&lt;/h2&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; container &lt;span class=&quot;token function&quot;&gt;ls&lt;/span&gt;
&lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; container logs web
&lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; container inspect web&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;ls&lt;/code&gt; 列出執行中的 container。&lt;/li&gt;
&lt;li&gt;&lt;code&gt;logs&lt;/code&gt; 查看主要 process 寫到標準輸出的 log。&lt;/li&gt;
&lt;li&gt;&lt;code&gt;inspect&lt;/code&gt; 查看完整設定、網路和 mount 資訊。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;把本機檔案掛進 container&lt;/h2&gt;
&lt;p&gt;先準備一個含 &lt;code&gt;index.html&lt;/code&gt; 的本機目錄，再執行：&lt;/p&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; container run &lt;span class=&quot;token parameter variable&quot;&gt;--name&lt;/span&gt; custom-web &lt;span class=&quot;token parameter variable&quot;&gt;-d&lt;/span&gt; &lt;span class=&quot;token punctuation&quot;&gt;&#92;&lt;/span&gt;
  &lt;span class=&quot;token parameter variable&quot;&gt;-p&lt;/span&gt; &lt;span class=&quot;token number&quot;&gt;8081&lt;/span&gt;:80 &lt;span class=&quot;token punctuation&quot;&gt;&#92;&lt;/span&gt;
  &lt;span class=&quot;token parameter variable&quot;&gt;--mount&lt;/span&gt; &lt;span class=&quot;token assign-left variable&quot;&gt;type&lt;/span&gt;&lt;span class=&quot;token operator&quot;&gt;=&lt;/span&gt;bind,source&lt;span class=&quot;token operator&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;token string&quot;&gt;&quot;&lt;span class=&quot;token environment constant&quot;&gt;$HOME&lt;/span&gt;/Documents/demo&quot;&lt;/span&gt;,target&lt;span class=&quot;token operator&quot;&gt;=&lt;/span&gt;/usr/share/nginx/html,readonly &lt;span class=&quot;token punctuation&quot;&gt;&#92;&lt;/span&gt;
  nginx&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;這個 bind mount 讓 nginx 讀取本機目錄內容。&lt;code&gt;readonly&lt;/code&gt; 可避免 container 修改本機檔案。路徑需要依自己的環境調整；正式資料持久化則應比較 bind mount 與 named volume 的差異。&lt;/p&gt;
&lt;h2&gt;停止與清理&lt;/h2&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; container stop web custom-web
&lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; container &lt;span class=&quot;token function&quot;&gt;rm&lt;/span&gt; web custom-web&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;停止 container 不會自動刪除它；不再需要時再以 &lt;code&gt;rm&lt;/code&gt; 清理。要查看包含已停止 container 的清單，可使用：&lt;/p&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token function&quot;&gt;docker&lt;/span&gt; container &lt;span class=&quot;token function&quot;&gt;ls&lt;/span&gt; &lt;span class=&quot;token parameter variable&quot;&gt;--all&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Image、Dockerfile 與 Container 的關係&lt;/h2&gt;
&lt;p&gt;Dockerfile 定義建置步驟，image 保存建置結果，container 則是 image 的執行實例。同一個 image 可以啟動多個彼此獨立的 container，但每個 container 的 runtime 狀態不同。&lt;/p&gt;
&lt;p&gt;回到 &lt;a href=&quot;https://imfw.io/posts/2026/2026-07-13-docker-basics-guide/&quot;&gt;Docker 基礎學習路線&lt;/a&gt;，或繼續了解 &lt;a href=&quot;https://imfw.io/posts/2020/2020-03-08-docker-basic-image/&quot;&gt;Docker Image&lt;/a&gt;。&lt;/p&gt;
&lt;h2&gt;官方參考資料&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.docker.com/get-started/docker-concepts/the-basics/what-is-a-container/&quot;&gt;Docker：What is a container?&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.docker.com/reference/cli/docker/container/run/&quot;&gt;docker container run&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.docker.com/engine/storage/bind-mounts/&quot;&gt;Bind mounts&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</content>
  </entry>
  
  <entry>
    <title>Docker - 基本概念</title>
    <link href="https://imfw.io/posts/2020/2020-02-16-docker-basic-concept/"/>
    <updated>2020-02-16T00:00:00Z</updated>
    <id>https://imfw.io/posts/2020/2020-02-16-docker-basic-concept/</id>
    <content type="html">&lt;p&gt;Docker 把應用程式和它需要的檔案、函式庫及設定包成 image，再以隔離的 container process 執行。它比完整 VM 輕量、啟動快，也容易在開發機、CI 和正式環境重現；但 container 通常與 host 共用 kernel，隔離模型和 VM 不相同。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;本文於 2026 年 7 月 13 日依目前 Docker 文件重新查核。不同作業系統上的 Docker Desktop 會使用虛擬化層，因此「與 host 共用 kernel」需要依實際平台理解。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2&gt;為什麼需要一致的開發環境&lt;/h2&gt;
&lt;p&gt;建立一個 Web Service 可能同時需要 Linux、Web server、資料庫和語言 runtime。直接把所有依賴裝在每位開發者的電腦上，容易遇到版本不一致、套件互相影響，以及「我的電腦可以跑」的問題。&lt;/p&gt;
&lt;p&gt;Container 讓每個服務以自己的隔離 process 執行，並由 image 定義執行時需要的檔案與設定。團隊可以在開發機、CI 和部署環境使用同一份 image，減少環境漂移。&lt;/p&gt;
&lt;h2&gt;Container 和 VM 的差異&lt;/h2&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;比較&lt;/th&gt;
&lt;th&gt;Container&lt;/th&gt;
&lt;th&gt;Virtual machine&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;隔離單位&lt;/td&gt;
&lt;td&gt;Process 與相關 namespace／resource&lt;/td&gt;
&lt;td&gt;完整 guest OS&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Kernel&lt;/td&gt;
&lt;td&gt;通常與 host 或虛擬化層共用&lt;/td&gt;
&lt;td&gt;Guest OS 有自己的 kernel&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;啟動與體積&lt;/td&gt;
&lt;td&gt;通常較快、較小&lt;/td&gt;
&lt;td&gt;通常較慢、較大&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;適合情境&lt;/td&gt;
&lt;td&gt;打包與執行應用服務&lt;/td&gt;
&lt;td&gt;需要完整 OS 或較強邊界的工作負載&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;在 macOS 或 Windows 使用 Docker Desktop 時，Linux container 實際上會跑在輕量 Linux VM 裡；因此不是直接拿 macOS 或 Windows kernel 執行 Linux container。&lt;/p&gt;
&lt;h2&gt;Docker 的核心物件&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Dockerfile&lt;/strong&gt;：描述如何建置 image 的文字檔。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Image&lt;/strong&gt;：包含執行 container 所需檔案、函式庫與設定的唯讀分層套件。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Container&lt;/strong&gt;：由 image 建立並正在執行或已停止的實例。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Registry&lt;/strong&gt;：儲存和分發 image 的服務，例如 Docker Hub。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;最簡化的流程是：撰寫 Dockerfile → build image → run container。&lt;/p&gt;
&lt;h2&gt;Docker 帶來的優勢&lt;/h2&gt;
&lt;h3&gt;重現環境&lt;/h3&gt;
&lt;p&gt;同一份 Dockerfile 與鎖定版本的 base image，可以建立可重複的應用環境。若需要完全固定內容，正式環境還可使用 image digest，而不只依賴會移動的 tag。&lt;/p&gt;
&lt;h3&gt;快速交付與回滾&lt;/h3&gt;
&lt;p&gt;image 可以經 registry 分發；部署與回滾時切換 image 版本，不必在伺服器上逐項重裝依賴。&lt;/p&gt;
&lt;h3&gt;提高資源使用效率&lt;/h3&gt;
&lt;p&gt;container 不需要為每個服務啟動完整 guest OS，通常能比 VM 更快啟動並使用更少額外資源。不過安全性、網路、儲存與資源限制仍需要明確設定，不能把 container 當成天然安全邊界。&lt;/p&gt;
&lt;h2&gt;下一步&lt;/h2&gt;
&lt;p&gt;按照 &lt;a href=&quot;https://imfw.io/posts/2026/2026-07-13-docker-basics-guide/&quot;&gt;Docker 基礎學習路線&lt;/a&gt;繼續閱讀：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;a href=&quot;https://imfw.io/posts/2020/2020-02-22-docker-basic-container/&quot;&gt;Docker Container 基本概念&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://imfw.io/posts/2020/2020-03-08-docker-basic-image/&quot;&gt;Docker Image 基本概念&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://imfw.io/posts/2020/2020-03-15-docker-basic-dockerfile/&quot;&gt;Dockerfile 基本概念&lt;/a&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;官方參考資料&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.docker.com/get-started/docker-concepts/the-basics/what-is-a-container/&quot;&gt;Docker：What is a container?&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.docker.com/get-started/docker-concepts/the-basics/what-is-an-image/&quot;&gt;Docker：What is an image?&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</content>
  </entry>
  
  <entry>
    <title>NVM - Node Version Manager 安裝</title>
    <link href="https://imfw.io/posts/2019/2019-03-19-nvm-install/"/>
    <updated>2019-03-19T00:00:00Z</updated>
    <id>https://imfw.io/posts/2019/2019-03-19-nvm-install/</id>
    <content type="html">&lt;p&gt;由於 node 版本越來越多，新舊版本有時候因為套件支援版本不同無法使用，在開發與維護上都是不少人的困擾，所以就有 NVM 誕生，來解決版本切換的問題&lt;/p&gt;
&lt;h2&gt;MAC&lt;/h2&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/creationix/nvm&quot;&gt;NVM&lt;/a&gt; 是由大神 &lt;a href=&quot;https://github.com/creationix&quot;&gt;Tim Caswell&lt;/a&gt; 所開發的一個專案，提供 linus/mac 使用的 Node 版本管理器，是目前最受歡迎的管理器&lt;/p&gt;
&lt;h4&gt;安裝方式&lt;/h4&gt;
&lt;p&gt;1.開啟 terminal 或是 iterm2 ，使用 curl 來下載並安裝 (不建議使用 homebrew )&lt;/p&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token function&quot;&gt;curl&lt;/span&gt; -o- https://raw.githubusercontent.com/creationix/nvm/v0.34.0/install.sh &lt;span class=&quot;token operator&quot;&gt;|&lt;/span&gt; &lt;span class=&quot;token function&quot;&gt;bash&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;安裝完之後，會有安裝資訊&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;第一個紅框代表安裝的路徑( /Users/XXX/.nvm )&lt;/li&gt;
&lt;li&gt;第二個紅框代表命令引入的資訊在.bashrc之中
&lt;ul&gt;
&lt;li&gt;有可能會在 ~/.zshrc, ~/.profile, or ~/.bashrc 之中&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;在最後安裝完成時可以輸入&lt;/p&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token builtin class-name&quot;&gt;export&lt;/span&gt; &lt;span class=&quot;token assign-left variable&quot;&gt;NVM_DIR&lt;/span&gt;&lt;span class=&quot;token operator&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;token string&quot;&gt;&quot;HOME/.nvm&quot;&lt;/span&gt;
&lt;span class=&quot;token punctuation&quot;&gt;[&lt;/span&gt; &lt;span class=&quot;token parameter variable&quot;&gt;-s&lt;/span&gt; &lt;span class=&quot;token string&quot;&gt;&quot;&lt;span class=&quot;token variable&quot;&gt;$NVM_DIR&lt;/span&gt;/nvm.sh&quot;&lt;/span&gt; &lt;span class=&quot;token punctuation&quot;&gt;]&lt;/span&gt; &lt;span class=&quot;token operator&quot;&gt;&amp;amp;&amp;amp;&lt;/span&gt; &lt;span class=&quot;token punctuation&quot;&gt;&#92;&lt;/span&gt;. &lt;span class=&quot;token string&quot;&gt;&quot;&lt;span class=&quot;token variable&quot;&gt;$NVM_DIR&lt;/span&gt;/nvm.sh&quot;&lt;/span&gt;
&lt;span class=&quot;token punctuation&quot;&gt;[&lt;/span&gt; &lt;span class=&quot;token parameter variable&quot;&gt;-s&lt;/span&gt; &lt;span class=&quot;token string&quot;&gt;&quot;&lt;span class=&quot;token variable&quot;&gt;$NVM_DIR&lt;/span&gt;/bash_completion&quot;&lt;/span&gt; &lt;span class=&quot;token punctuation&quot;&gt;]&lt;/span&gt; &lt;span class=&quot;token operator&quot;&gt;&amp;amp;&amp;amp;&lt;/span&gt; &lt;span class=&quot;token punctuation&quot;&gt;&#92;&lt;/span&gt;. &lt;span class=&quot;token string&quot;&gt;&quot;&lt;span class=&quot;token variable&quot;&gt;$NVM_DIR&lt;/span&gt;/bash_completion&quot;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;來直接載入 nvm 的指令，或是重新啟動 terminal 來載入&lt;/p&gt;
&lt;p&gt;成功!!&lt;/p&gt;
&lt;h4&gt;常用指令&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;nvm run node [version] --- 切換不同版本&lt;/li&gt;
&lt;li&gt;nvm install [version]  --- 安裝不同版本&lt;/li&gt;
&lt;li&gt;nvm ls                 --- 查看已安裝的版本&lt;/li&gt;
&lt;li&gt;nvm ls-remote          --- 查看可以安裝的版本&lt;/li&gt;
&lt;/ul&gt;
&lt;h4&gt;解除安裝方式&lt;/h4&gt;
&lt;p&gt;輸入指令&lt;/p&gt;
&lt;pre class=&quot;language-bash&quot;&gt;&lt;code class=&quot;language-bash&quot;&gt;&lt;span class=&quot;token function&quot;&gt;rm&lt;/span&gt; &lt;span class=&quot;token parameter variable&quot;&gt;-rf&lt;/span&gt; &lt;span class=&quot;token variable&quot;&gt;$NVM_DIR&lt;/span&gt; ~/.npm  ~/.bower&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;然後將命令資訊也刪掉&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;有可能會在  ~/.zshrc, ~/.profile, or ~/.bashrc 之中&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;就可以將nvm完全刪除囉!&lt;/p&gt;
&lt;h2&gt;Windows&lt;/h2&gt;
&lt;p&gt;雖然 NVM 並沒有提供 Windows 版本，不過還是有大神 &lt;a href=&quot;https://github.com/coreybutler&quot;&gt;Corey Butler&lt;/a&gt; 提供了 &lt;a href=&quot;https://github.com/coreybutler/nvm-windows&quot;&gt;nvm-windows&lt;/a&gt; 為 Windows 提供一樣好用的 Node 版本管理器，而且安裝的方式相當簡單，可以在這個專案的 &lt;a href=&quot;https://github.com/coreybutler/nvm-windows/releases&quot;&gt;release&lt;/a&gt; 中下載安裝檔(.exe)&lt;/p&gt;
&lt;h4&gt;安裝方式&lt;/h4&gt;
&lt;p&gt;下載 nvm-setup.zip 解壓縮就可以 GET 安裝檔(.exe)
點兩下開始安裝!!!&lt;/p&gt;
&lt;p&gt;選擇安裝路徑&lt;/p&gt;
&lt;p&gt;選擇一個存放進入點(捷徑)的路徑並且會自動加入環境變數&lt;/p&gt;
&lt;p&gt;就可順利安裝啦!!!&lt;/p&gt;
&lt;p&gt;打開 cmder 測試一下( 命令提示字元 cmd 、 powershell 都可以使用)&lt;/p&gt;
&lt;p&gt;輸入 : &lt;code&gt;nvm -h&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;有跑出版本資訊與使用說明就是安裝成功啦!&lt;/p&gt;
&lt;p&gt;一樣可以使用&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;nvm use [version]     --- 切換不同版本&lt;/li&gt;
&lt;li&gt;nvm install [version] --- 安裝不同版本&lt;/li&gt;
&lt;li&gt;nvm list              --- 查看已安裝的版本&lt;/li&gt;
&lt;li&gt;nvm list available    --- 查看可以安裝的版本&lt;/li&gt;
&lt;/ul&gt;
&lt;h4&gt;解除安裝方式&lt;/h4&gt;
&lt;p&gt;利用 windows 的新增或移除程式刪除即可!!&lt;/p&gt;
&lt;h4&gt;注意&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;nvm 與 nvm-windows 是不同的套件&lt;/li&gt;
&lt;/ul&gt;
</content>
  </entry>
  
</feed>
